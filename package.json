{
  "name": "affordmed-backend",
  "version": "1.0.0",
  "description": "Vehicle Maintenance Scheduler with Notification System",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "inbox": "node --env-file=.env notification_app_be/priorityInbox.js"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
